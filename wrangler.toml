name = "fluxaipor1"
main = "src/index.js"
compatibility_date = "2024-12-24"
compatibility_flags = ["nodejs_compat"]

[vars]
PROJECT_NAME = "Flux AI Pro"
PROJECT_VERSION = "3.4.0"

# API Configuration (可選配置)
# POLLINATIONS_API_KEY = ""  # 如需使用 API Key，請取消註釋並填入

# 移除不需要的綁定（當前版本不使用 R2/KV/AI）
# 如果將來需要持久化存儲，可以重新啟用這些配置
